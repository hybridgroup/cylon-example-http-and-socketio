# Cylon.js - HTTP + SocketIO Example

This repo contains a basic example Cylon robot, and an example integration of the HTTP and SocketIO api plugins.

## Running

    $ npm install
    $ node script.js

Now, visit `http://localhost:3000`.
